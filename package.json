{
  "name": "azurenode",
  "version": "1.0.0",
  "description": "this is for testing nodejs app on Azure",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "Express": "^3.0.1"
  }
}
